pipeline {
    agent any
    
    stages {
        stage('checkout') {
            steps {
                git url: 'https://github.com/sdineshgandhi/k8s-demo-deploy1.git', branch: 'main'
            }
        }
    }
}